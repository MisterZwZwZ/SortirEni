{% extends 'base.html.twig' %}

{% block title %}Profil de {% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <h1>profil de {{ profil.pseudo }}</h1>

<div class="example-wrapper">
    <div class="list-group">
{#        <div class="list-group-item list-group-item-action active">Détails du souhait: {{ wish.id }}#}
{#            {% if wish.user.id == app.user.id or is_granted("ROLE_ADMIN") %}#}
{#                (<a style="color:white" href="{{ url('wish_edit', {"id": wish.id }) }}">Edit</a> - <a style="color:white" href="{{ url('wish_remove', {"id": wish.id }) }}">Supprimer</a>)#}
{#            {% endif %}#}
{#        </div>#}
        <div class="list-group-item list-group-item-action">Prenom : {{ profil.prenom }}</div>
        <div class="list-group-item list-group-item-action">Nom : {{ profil.nom }}</div>
        <div class="list-group-item list-group-item-action">Téléphone : {{ profil.telephone}}</div>
        <div class="list-group-item list-group-item-action">Email : {{ profil.email }}</div>
        <div class="list-group-item list-group-item-action">Campus : {{ profil.campusUser.nomCampus }}</div>
    </div>
    <button type="submit" onclick="history.go(-1)" class="btn btn-lg btn-primary">Retour</button>
</div>
{% endblock %}
