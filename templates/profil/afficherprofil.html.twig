{% extends 'base.html.twig' %}

{#{% block title %} {% endblock %}#}
{% block taille %}w-50{% endblock taille %}
{% block title_2 %}Profil de {{ profil.pseudo }}{% endblock title_2 %}
{% block body %}
{#    <style>#}
{#        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }#}
{#        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }#}
{#    </style>#}
{#    <h1>profil de {{ profil.pseudo }}</h1>#}

<div class="example-wrapper">
    <div class="row">
        <div class=""><label>Prenom : </label><p>{{ profil.prenom }}</p></div>
        <div class=""><label>Nom : </label><p>{{ profil.nom }}</p></div>
        <div class=""><label>Téléphone : </label><p>{{ profil.telephone}}</p></div>
        <div class=""><label>Email : </label><p>{{ profil.email }}</p></div>
        <div class=""><label>Campus : </label><p>{{ profil.campusUser.nomCampus }}</p></div>
    </div>
    <div class="mt-3">

        <button class="btn btn-primary"><a class="customize" href="{{ path('default_accueil') }}">Retour</a></button>

{#        <button type="submit" href="{{ url('default_accueil') }}" class="btn btn-lg btn-primary">Retour</button>#}
    </div>
</div>
{% endblock %}
